export class CreateDynamicDTO {
  readonly title: string;
  readonly image: string;
  readonly desc: string;
  readonly html: string;
  readonly date: number;
  readonly dateStr: string;
}

export class SaveDynamicDTO {
  readonly title: string;
  readonly image: string;
  readonly desc: string;
  readonly html: string;
  readonly date: number;
  readonly dateStr: string;
}
